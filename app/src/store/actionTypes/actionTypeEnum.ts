enum ActionType {
  SET_ALL_RESTAURANT_NAMES = 'set all restaurant names for searching',
  SET_SEARCH_FIELD = 'set the current name of restaurant',
  CLEAN_SEARCH_FIELD = 'clean the input field',

  SET_CURRENT_DATETIME = 'set current datetime to filter',
}

export default ActionType;
